# Automated Gmail Login
# Instructions

1. Install Python 3.6 (https://www.python.org/ftp/python/3.6.4/python-3.6.4.exe)
2. Install Selenium (in cmd -> pip.exe install selenium)
3. Download Chrome web driver.exe (https://chromedriver.storage.googleapis.com/index.html?path=2.35/)
4. Use script with Python IDLE or PyCharm (https://www.jetbrains.com/pycharm/download/download-thanks.html?platform=windows&code=PCC)
5. Input your email and password in "user" and "pwd"
6. In driverpath input your path to chromdriver.exe
6. Run

# Import 
import time
from selenium import webdriver
from selenium.webdriver.common.keys import Keys

# Username
user = "youremail@gmail.com"

# Password
pwd = "yourpassword"

# Chrome Driver
driverpath = "path/to/chromedriver.exe" # put your path to chromedriver 

# Open Chrome
driver = webdriver.Chrome(driverpath)
driver.maximize_window()

# Open Gmail Page
driver.get("http://www.gmail.com")
time.sleep(2)

# Input Username or Email
element = driver.find_element_by_id("identifierId")
element.send_keys(user)
time.sleep(1)

# Press next for password
next = driver.find_element_by_xpath("//*[@id='identifierNext']/content/span").click()
time.sleep(1)

# Enter Password
element = driver.find_element_by_xpath("//*[@id='password']/div[1]/div/div[1]/input")
element.send_keys(pwd)
time.sleep(1)

# Login Button
driver.find_element_by_xpath("//*[@id='passwordNext']/content/span").click()
time.sleep(5)

# Close Browser
driver.close()
